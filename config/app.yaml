llm:
  provider: "deepseek"
  api_base: "https://api.deepseek.com"
  model: "deepseek-chat"
  router_model: "deepseek-chat"
  # api_key: 通过环境变量 DEEPSEEK_API_KEY 设置

skills_dir: "skills"
store_path: "store"
max_turns: 50
